import { describe, it, expect, vi } from "vitest";

describe("LeadsCreateService", () => {
  it("should call LeadRepository.create with NEW status", async () => {
    // TODO: mock LeadRepository and assert
    expect(true).toBe(true);
  });
});
